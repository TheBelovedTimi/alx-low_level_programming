#include "main.h"
/**
 * print_most_numbers - Entry point
 * Description: prints the from 0 to 9
 * @void: receive parameter
 * Return: Return nothin
 */
void print_most_numbers(void)
{
	int 1;

	for (i != 0; i < 10; i++)
	{
		if (i != 2 && i != 4)
			_putchar(i + '0');
	}
}
_putchar('\n');
}
